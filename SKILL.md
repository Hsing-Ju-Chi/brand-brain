---
name: brand-brain
description: Use when 使用者要 AI 做出符合自家品牌的簡報：說「建品牌大腦」「用我的品牌做簡報」「簡報要像我們的樣子」，抱怨 AI 簡報每次長得不一樣、做完還要一頁頁調，或丟出品牌色、logo、舊簡報、參考品牌（「像法拉利那種配色」）要 AI 學風格時。
---

# 簡報品牌大腦 Brand Brain

> 出處：阿幸（IG @hsing.daily）。
> 裝法：① Claude Code：整個資料夾放進 `~/.claude/skills/`；② Codex、Gemini CLI：放進 `~/.agents/skills/`（兩家共用同一個位置）；③ Claude 網頁版或 App：壓成 ZIP 在「自訂 → Skills」上傳（雲端沙盒：規則全在，但查不到本機字型、不燒 PPTX）；④ ChatGPT Business／Enterprise／Edu：在 Skills 上傳；其他 ChatGPT 方案與 Gemini 網頁版：把本檔全文（含 references/ 兩檔）貼進自訂指令或 Gem。

## 核心原則

**方案你先想好，使用者只回答問題。** 每一題都附選項或建議；使用者說「你決定」「都可以」，剩下的全部用預設補完，不再問。先建大腦（一份規則檔），再做簡報（照檔案做，不即興）。

## 現在該走哪個模式

- 說「建品牌大腦」「幫我整理品牌風格」「照這份簡報的風格」，或還沒有 `品牌大腦-*.md` → **模式一**
- 說「用範本『粉彩玩趣』開始」「直接用範本 X」 → **模式一捷徑**：複製 `templates/examples/` 對應那份，只問第 0 節（品牌與對象），其餘照範例
- 有品牌大腦檔＋簡報內容 → **模式二**
- 說「品牌改色了」「換字型」「改哪條規則」 → **模式三**

## 模式一：建大腦（三題建完）

「三題」＝下面編號 1、2、3。第 0 步只是收東西，不算題；題目裡的確認（「這組可以嗎」）併在同一則訊息裡問，不另開一題。

**第 0 步：先收資產，拿到就先抽。** 問一句：「手上有 logo、舊簡報、品牌規範、官網，或想參考的簡報嗎？有什麼給什麼。」使用者開場就附了東西 → 不問這句，直接抽，抽完在第 1 題的訊息裡帶一句「其他資產（logo 等）之後有再給」。抽得到的不再問：

| 拿到什麼 | 抽什麼 |
|---|---|
| 自家舊簡報、截圖、PDF | **只抽六樣重點**（見下）；標「從簡報抽出的估計值」 |
| 別人的簡報當參考 | 同六樣，但照color-presets.md「從參考品牌抽色」的規矩：標參考值、靈感非複製、不用對方 logo 與圖形 |
| logo | 主色與強調色 |
| 品牌規範 | 全部照規範，不加不減 |
| PPTX 檔 | 本機終端環境（Claude Code、Codex、Gemini CLI）直接讀主題色與字型（`python-pptx` 或解壓讀 `ppt/theme/theme1.xml`）；網頁版或沙盒環境請使用者改給 PDF 或截圖 |

**從簡報只抽這六樣，寫進大腦就丟掉原檔：**
1. 背景底稿：底色，有沒有固定的色帶、邊框、幾何圖形
2. 標題區樣式：位置（左上／左下／置中）、有沒有底線或色塊、跟內文的字級關係
3. 整體用色：四個角色（主色、背景、文字、強調）
4. footer 與頁碼：位置、放什麼（頁碼、品牌名、日期）、字級
5. 章節頁樣式：章節頁或轉場頁長什麼樣（反白底色、大數字、一句話）
6. 字型：認得出寫名字，認不出寫類型（黑體／宋體／圓體／手寫）

**不做的事：不仿造原簡報的每一頁版面，不重畫它的圖表和插圖。** 仿造整份比重做更花時間、效果更差；大腦要的是規則，不是複製品。六樣以外的東西（每頁怎麼排、圖表長怎樣）一律交給版型預設。

**抽到的優先。** 六樣裡抽到的值直接寫進大腦，範本只補沒抽到的欄位；使用者說「照這份的風格」時不要用範本值蓋掉抽到的值。從檔案讀到精確色碼就標「從檔案讀出」，從截圖估的才標「估計值」。抽到字型一樣走第 3 題的查證流程（本機、Google Fonts），但不再問他。

什麼都沒有也沒關係，往下走。

**第 1 題：品牌與對象。** 「品牌是做什麼的？簡報通常給誰看？」一句話就好。

**第 2 題：用色系統。** 依使用者的狀況走其中一條：

| 使用者狀況 | 你怎麼做 |
|---|---|
| 已有色系（主色、輔色，或整份規範），或第 0 步已從簡報抽出 | 引導他給你：主色、輔色、背景、文字。輔色就放強調色的位置；再多的顏色不進四色，要用先問。缺的由你補齊並附理由請確認。版型預設依主色挑（color-presets.md「版型怎麼配」）。 |
| 沒有色系、講得出感覺或簡報用途 | 給 `references/color-presets.md` 8 款代表範本讓他挑（每款一句場景＋四個色碼＋底稿風格）。挑完可微調。 |
| 舉例參考（「像法拉利 F1」「像 Muji」） | 照color-presets.md「從參考品牌抽色」五步：講印象 → 抽四色（標參考值非官方）→ 明講靈感非複製 → 講落差 → 配最近的版型預設。 |

收尾一律輸出四個角色（主色／背景／文字／強調）＋ 60-30-10 比例。

**第 3 題：字型。** 只問「有指定字型嗎？」

- 有指定 → **先把中文名換成英文檔名再查**（字型檔名都是英文，直接 grep 中文一定查無）：思源黑體＝`SourceHanSans`／`NotoSansCJK`／`NotoSansTC`；思源宋體＝`SourceHanSerif`／`NotoSerifCJK`／`NotoSerifTC`；台北黑體＝`TaipeiSans`；粉圓＝`Huninn`；霞鶩文楷＝`LXGWWenKai`；其他字型問使用者英文名。**查本機**：本機終端環境（Claude Code、Codex、Gemini CLI）跑 `ls ~/Library/Fonts /Library/Fonts /System/Library/Fonts | grep -i 英文名`（macOS）；網頁版或沙盒環境問使用者「這個字型你電腦有裝嗎」。有裝 → `@font-face` 用 `local()`，並找 Google Fonts 當後備。沒裝 → 查 Google Fonts（一樣用英文名）：對 `https://fonts.googleapis.com/css2?family=英文名` 發請求，回 200 就線上載入，回 400 就從範本檔挑最接近的免費字型代替，**明講「這是代替，正式版請自行替換」**。
- 沒指定 → 從 color-presets.md 給 2 個組合，一句差異，讓他選。

**排版不問。** 留白、對齊、圓角、陰影、行距、字級、每頁字量全部跟著範本的版型預設走；使用者要改哪條，直接說一句就改。

**收尾規則：**
- 使用者說「你決定」「都可以」→ 剩下全部套預設，一次補完，不再問。風格類欄位都有預設：版型＝範本；icon＝Lucide，粗細照該範本範例大腦的值（沒有範本就 2）；插圖＝塗鴉手繪／粉彩玩趣／紙本雜誌走手繪感，其他範本走扁平線條；照片方向＝照範本氣質寫一句；底稿風格＝範本預設配給（color-presets.md「底稿風格庫」：簡潔提案→角標細線、塗鴉手繪→紙紋塗鴉、玻璃科技→玻璃擬態、紙本雜誌→紙本編輯、粉彩玩趣→粉彩色塊、靜物極簡→純色極簡、螢光撞色→撞色分割、便當格→便當格）；標題區、footer、章節頁抽不到時用 color-presets.md「共用固定值」的預設；禁區＝範本例句。
- `TODO` 只用在**只有使用者才知道的事實**：logo、聯絡方式、他指定但還沒給的色碼或字型。這類不准自己編一個看起來合理的值。
- 使用者跳題（問字型他回答顏色）→ 先接他的，再回原題。
- 產出 `品牌大腦-{品牌名}.md`（格式照 `templates/brand-brain-template.md`），提醒：**存起來，之後每次做簡報先貼它。** 用網頁版 AI 的人，教他把整份貼進 ChatGPT 的自訂指令或 Project、Claude 的 Project、Gemini 的 Gem，之後開新對話不用重貼也不用重答。

## 範例（想先看長什麼樣）

- `templates/examples/brand-brain-{pro|warm|tech|nature|vivid|luxe|neon|bento}.md`：八份填好的大腦（8 款代表範本），換掉第 0 節就能用。
- `examples/{pro|warm|tech|nature|vivid|luxe|neon|bento}.html`：八份十頁式範例簡報，各配一種底稿風格與封面構圖，頁型：Design System → 封面 → 章節頁 → 三欄重點 → 左文右圖 → 長文頁 → 一頁一數字 → 章節頁 → 步驟流程 → 封底。瀏覽器打開就能看；同名 `.pptx` 是燒圖管線做出來的可編輯版。線上版：https://hsing-resource-center.vercel.app/resources/ai-brand-brain
- `scripts/pptx_from_spec.py`：PPTX 燒圖管線的組頁腳本（用法見 `references/pptx-bake.md`）。

## 模式二：做簡報

輸入：品牌大腦檔＋這次的內容（大綱、文章、逐字稿都行）。只給頁名沒給內容 → 用【待填】標示，**不編數字、不編事實**。

輸出：預設交可編輯的 PPTX（走下面的燒圖管線）並附 HTML 原稿；使用者指定只要 HTML，或要 Google Slides，就照說的給（Google Slides：交 PPTX，請使用者上傳雲端硬碟用 Slides 開啟）。HTML 規格：16:9，每頁 1280×720，單一檔案；字型走 Google Fonts 或 `local()`；icon 以內嵌 SVG 放進檔案；使用者提供的圖放 `assets/`；瀏覽器打開就能全螢幕播，也能印成 PDF。

PPTX 怎麼做：本機終端環境（Claude Code、Codex、Gemini CLI）有 `python3` 和 Chrome → 走 `references/pptx-bake.md` 的燒圖管線（底稿層燒成圖鋪滿，文字、卡片框、icon 原生可編輯；手繪線稿的卡片框是風格本體，跟底稿一起燒進圖；`scripts/pptx_from_spec.py` 直接用），HTML 一併給；有 python 沒 Chrome（例如 Claude 網頁版沙盒）→ 用 `python-pptx` 出無底稿的 PPTX，交件時列出底稿沒做到的部分；兩者都沒有 → 給 HTML，並說明可用瀏覽器印成 PDF。燒圖管線目前只在 Claude Code 驗過。

**硬規則：**
1. 顏色只准用色票四色。要新色，先問。
2. 字型照大腦載入、附 fallback；字級全簡報一套。
3. 版型預設照做，含垂直分布：內容不到半頁時，重點區垂直置中或字級放大，不留空半頁。字級全簡報一套、不准縮；一頁講一件事，內文超過一個版面裝不下，或超過三個並列重點，就拆頁，或改用頁型庫裡能並列更多重點的頁型，不縮字硬塞。
   頁型照大腦的「頁型庫」排：**第 1 頁 Design System**（色票、字型、底稿、元件、場景、頁型六格）、封面、章節頁（每個大段落前一頁）、三欄重點、左文右圖、長文頁（內文多時用，字級仍不縮）、一頁一數字、步驟流程（有順序才用編號）、封底（一句話＋聯絡方式）。內容多就重複內容頁型，不發明新頁型；長什麼樣看 `examples/` 對應範本那份。
   底稿照大腦的「底稿風格」做成整份每頁共用的一層（CSS 寫法見 color-presets.md「底稿風格庫」），文字區的底稿元素透明度不超過 .3。
4. 大腦裡的 `TODO` 用到時：交付先放佔位（虛線框＋「待提供」），在交付訊息裡問，不卡交付、不猜。
5. **資產照 `references/asset-licenses.md`：**
   - icon：一份簡報一套（預設 Lucide），抓 SVG 內嵌，`stroke` 用主色，粗細統一。
   - 插圖：依大腦的圖片風格挑同一庫；每張給「放哪頁＋搜尋關鍵字＋去哪個庫＋改成哪個色碼」。表裡第二區的庫（unDraw 等）**不准自動抓**，讓使用者下載後放 `assets/`，你再嵌入。
   - 照片：先放主色色塊佔位＋標搜尋詞。
   - 不在授權表裡的來源，不用。

**交付前自我檢查，附在回覆裡：**
- [ ] 用色都在色票四色內
- [ ] 字型載入正確、有 fallback、字級一套
- [ ] 沒有頁面因為塞字而縮了字級
- [ ] 留白、對齊、圓角、陰影照版型預設
- [ ] icon 同一套、同粗細
- [ ] 每張圖片的來源都在授權表內

## 模式三：更新大腦

改對應欄位＋「最後更新」日期，其他不動，附一句「改了哪裡」。不整份重建。「底稿換手繪」這種，只改第 3 節「底稿風格」那一行，做簡報時整份重出（底稿是每頁共用的層，不能只換一頁）。

## 邊界

- 一個品牌一份檔；多品牌多份，做簡報前先確認用哪份。
- 你負責「像品牌」，不負責「內容正確」：數字、事實提醒使用者自行確認。
- 參考品牌只當靈感：不用對方 logo、字型、圖形。
